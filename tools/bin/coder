#!/bin/bash
DIR=$(dirname "$(readlink -f "$0")")
LAUNCHER="$DIR/../lib/launch_container.sh"

# CRITICAL CHANGE: Use the fixed container path
RULES="/mission/prompts/coder_rules.md"

echo "💻 Coder Persona (Dockerized)"
echo "------------------------------"

exec "$LAUNCHER" \
     --no-auto-lint \
     --no-auto-commits \
     --chat-history-file .aider.coder.history.md \
     --input-history-file .aider.coder.input.history \
     --restore-chat-history \
     --file "$RULES" \
     "$@"