#!/bin/bash
DIR=$(dirname "$(readlink -f "$0")")
LAUNCHER="$DIR/../lib/launch_container.sh"

echo "💻 Coder Persona (Dockerized)"
echo "------------------------------"

exec "$LAUNCHER" \
    --no-auto-commits \
    "$@"
