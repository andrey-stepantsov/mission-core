#!/bin/bash
DIR=$(dirname "$(readlink -f "$0")")
LAUNCHER="$DIR/../lib/launch_container.sh"

echo "🧠 Architect Persona"
echo "------------------------------"

exec "$LAUNCHER" \
    --architect \
    --yes \
    "$@"
